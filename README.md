# Diffusion Backdoor Embed

for cifar10
```shell
python sandiffusion.py --config-name cifar10 attack=badnet ratio=0.3
```

gtsrb
```shell
python sandiffusion.py --config-name gtsrb attack=badnet ratio=0.3
```

celeba
```shell
python sandiffusion.py --config-name celeba attack=badnet ratio=0.3
```

